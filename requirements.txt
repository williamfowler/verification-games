# Ported to x86 dual-Tesla-V100. Standard PyPI torch (cu12x wheels include sm_70,
# so they run on the V100) — NOT the Jetson AI Lab wheel index. The DRAM signal
# comes from DCGM (system `datacenter-gpu-manager` / `dcgmi` + `nv-hostengine`),
# not a Python package. jetson-stats is not used on this platform.
torch
numpy
matplotlib
